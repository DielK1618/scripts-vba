# SOURCES.md — tpl_ 소스 함수 목록 (xlam 이식 대상)

> 소스 파일 위치: `refer/export/*.bas`
> 작업 시 해당 .bas 파일을 Read — vba.xlsm 은 더 이상 열 필요 없음

> 실제 소스는 `export/*.bas` 에 있음 — 작업 시 해당 파일 Read
> vba.xlsm 은 더 이상 열 필요 없음

---

## am_Core ← tpl_Const + Common.cls

### Common.cls
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `DPUpdate_Off/On` | ✅ | 화면 업데이트 + 경고창 토글 |
| `Event_Off/On` | ✅ | 이벤트 토글 |
| `Calculate_Off/On` | ✅ | 계산 모드 토글 |
| `WB_Lock/UNLock` | ✅ (수정) | PW 인수로 받도록 변경 |
| `sht_Lock` | ⬜ 재설계 | blnPartEvents·wsPub·PW·GetUsedRange·FindCellsByColor 의존 — xlam 형태로 재설계 필요 |
| `sht_UnLock` | ⬜ 재설계 | wsPub·PW 의존 |

---

## am_Core ← tpl_Const

| 함수/선언 | 이식 여부 | 비고 |
|---|---|---|
| `DBvar` (Type) | ✅ → am_DB | DB 작업 구조체 |
| `uiColor` (Enum) | ❌ 제외 | 특정 CWB 전용 테마컬러, xlam 범용 상수로 부적합 |
| `Public cl As New Common` | ❌ 제외 | vba.xlsm 전용 클래스 |
| `Public Const PW` | ❌ 제외 | 특정 워크북 비밀번호 |
| `dicPub / dicPubRev` | ❌ 제외 | am_DB 내부로 이동 |
| `wsPub / blnPartEvents / vntTemp` | ❌ 제외 | CWB 전용 전역변수 |
| `CM_TO_POINTS` | ✅ → am_Core | 단위 변환 상수 |

---

## am_Error ← tpl_Error

| 함수/선언 | 이식 여부 | 비고 |
|---|---|---|
| `HandleError` | ✅ (수정 필요) | `cl.*` 호출 제거, am_Core 패턴으로 교체 |
| `WriteLog` | ✅ (수정 필요) | `ThisWorkbook.Path` → `am_Core.XlamPath` |
| `ENABLE_ERROR_LOG` | ✅ | 상수 유지 |

---

## am_DB ← tpl_MsSQL + tpl_MySQL_Sub + tpl_Access

### tpl_MsSQL
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `conStr` | ✅ (수정 필요) | `cfg_*` → 인수로 받도록 변경 |
| `ExecuteQueryArr` | ✅ | 재시도 로직 포함, 우수 |
| `SelectQuery` | ✅ | CopyFromRecordset 방식 |
| `SelectQueryArr` | ✅ | 배열 반환 방식 |
| `DelExcelRecQuery` | ✅ | 엑셀 행 삭제 유틸 |
| `GetDbTables` | ✅ | DB 테이블 목록 조회 |
| `GetFieldInfo` | ✅ | 필드 스키마 조회 |
| `IsAutoGeneratedFieldName` | ✅ | 보조 함수 |
| `GetDataTypeName` | ✅ | 보조 함수 |
| `GetDataTypeCode` | ✅ | 보조 함수 |
| `AutoIDs` | ✅ | am_DB 하단 배치 |
| `FormatValueForSQLByDBType` | ✅ | SQL 값 포맷 변환 |
| `ValidateValueForDBType` | ✅ | DB 타입 검증 |
| `ConvertRangeToSQLWithDBTypes` | ✅ | 범위 → SQL 변환 |
| `GetValueValidationReport` | ❌ 제외 | 이미 설계 결정에서 제거 |

### tpl_MySQL_Sub
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `PublicODBC_Driver / PublicExcel_Ver / PublicAccess_Ver` | ❌ 제외 | am_DB에 이미 존재 |
| `cfg_*` Property Get | ❌ 제외 | CWB 시트 참조 종속 |
| `GetCfgDbInfo` | ❌ 제외 | CWB 시트 참조 종속 |
| `GetDbInfo` | ❌ 제외 | CWB 시트 참조 종속 |
| `ReloadDbInfo` | ❌ 제외 | CWB 종속 |
| `GetFieldMap / GetFieldMapRev` | ❌ 제외 | dicPub/dicPubRev CWB 전역변수 종속 |
| `ReplaceFields` | ❌ 제외 | dicPub 종속 |
| `GetFields` | ✅ (수정 필요) | 색상 필터로 헤더 배열 반환, 범용 |
| `FormatValueForSQLByDBType` | ❌ 제외 | tpl_MsSQL에 이미 있음 |
| `ValidateValueForDBType` | ❌ 제외 | tpl_MsSQL에 이미 있음 |
| `UpsertQuery` | ✅ | SQL INSERT/UPDATE/DELETE 생성 |
| `GetFieldAndType` | ✅ (수정 필요) | ReplaceFields 의존성 제거 필요 |
| `RecDBUpdateDateTime` | ❌ 제외 | CFG_DB 토큰 종속 |
| `GetFieldNameConnection` | ✅ (수정 필요) | ReplaceFields 의존성 제거 필요 |
| `asdf*` 테스트 Sub들 | ❌ 제외 | 테스트 코드 |

### tpl_Access
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `CreateAccessTable` | ✅ | SQL 방식 테이블 생성 |
| `DeleteAccessTable` | ✅ | 테이블 데이터 삭제 |
| `CreateAccessTableADOX` | ✅ | ADOX 방식 (안전, 기존 보존) |
| `DeleteAccessFields` | ✅ | 특정 필드 삭제 |
| `Example_*` Sub들 | ❌ 제외 | 예시 코드 |
| `DiagnoseAccessConnection` | ❌ 제외 | 진단용 특수 코드 |

---

## am_Range ← tpl_Find + tpl_Range

### tpl_Find
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `FindCellsByColor` | ✅ | 배경색으로 셀 찾기, 범용 |

### tpl_Range
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `GetTwbRange` | ❌ 제외 | ThisWorkbook 활성화 방식, CWB 종속 |
| `GetUsedRange` | ✅ | 실제 사용 범위 계산, 범용 |

---

## am_Excel ← tpl_Chart + tpl_ExportFile + tpl_KeyBoard + tpl_Mouse + tpl_Shapes

### tpl_Chart
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `SetChartDataRange` | ✅ | 차트 데이터 범위 설정 |

### tpl_ExportFile
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `SetPrintPage` | ✅ | 인쇄 설정, 완성도 높음 |
| `ExportPDF` | ✅ | PDF 내보내기 |
| `ExportSheetToCSV` | ✅ (수정 필요) | `Call MkFolder` → prv_MkFolder 내부 구현 |

### tpl_KeyBoard
| 함수/선언 | 이식 여부 | 비고 |
|---|---|---|
| `keybd_event` API | ✅ | Windows API |
| `VirtualKeys` Enum | ✅ | 가상 키코드 |
| `KeyActions` Enum | ✅ | 키 동작 정의 |
| `ExecuteKeyAction` | ✅ | 키 동작 실행 |
| `ExecuteKeySequence` | ✅ | 키 시퀀스 실행 |
| `ProcessRangeWithKeySequence` | ✅ | 범위에 키 시퀀스 적용 |
| `SendKey / SendKeyCombo / Wait` | ✅ → prv_ | 내부 전용으로 변경 |

### tpl_Mouse
| 함수/선언 | 이식 여부 | 비고 |
|---|---|---|
| `GetCursorPos` API | ✅ | Windows API |
| `POINTAPI` Type | ✅ | 좌표 구조체 |
| `mouse_event / SetCursorPos` API | ✅ | Windows API |
| `GetMousePosition` | ✅ | 마우스 위치 반환 |
| `ClickAtPosition` | ✅ | 좌표 클릭 |
| `WaitTime` | ✅ | 대기 유틸 |
| `GetMousePosition_BT` | ❌ 제외 | 테스트용 서브 |

### tpl_Shapes
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `RunShpMacro` | ✅ | 도형 텍스트로 매크로 실행 |
| `GetShapeTextSafe` | ✅ | 도형/그룹 텍스트 안전 반환 |
| `GetShapeTextSafe_GItem` | ✅ → prv_ | 내부 전용으로 변경 |
| `ExampleRunShpMacro` | ❌ 제외 | 예시 코드 |

---

## am_Utils ← tpl_Array + tpl_Check + tpl_Code + tpl_ExtApp + tpl_Media + tpl_Procedure + tpl_ReplaceText + tpl_Tools + tpl_Validation

### tpl_Array
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `ConvertToArrData` | ✅ | 중첩 배열 → 평탄화 |
| `FlattenArray` | ✅ → prv_ | 내부 전용 |
| `CountElements` | ✅ → prv_ | 내부 전용 |

### tpl_Check
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `AccessTableExists` | ✅ → am_DB | ADOX로 테이블 존재 확인 → am_DB 배치 적합 |
| `IsTableRange` | ✅ | ListObject 범위 확인 |
| `GetValidationType` | ✅ | 유효성 검사 타입 문자열 반환 |
| `IsArrayEmpty` | ✅ | 배열 비어있는지 확인 |
| `IsCells` | ✅ | Range 타입 확인 |
| `IsValidFileName` | ✅ | 파일명 유효성 검사 |
| `IsRangeMerged` | ✅ (버그수정) | `IsSelectionMerged` → `IsRangeMerged` 오타 수정 필요 |

### tpl_Code
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `BtCreateCodes` | ❌ 제외 | 시트 셀 직접 참조 ([E3] 등) CWB 전용 |
| `GetExistingCodes` | ❌ 제외 | DBvar/GetDbInfo 종속, CWB 전용 |
| `CreateUniqueID` | ✅ | 접두사+숫자 고유ID 생성, 범용 |
| `CheckUniqueID` | ✅ → prv_ | 내부 전용 |
| `GenerateRandomCode` | ✅ | 랜덤 코드 생성, 범용 |

### tpl_ExtApp
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `OpenAddressInGoogleMaps` | ✅ | 주소 → 구글맵 열기 |
| `GetVideoLength` | ✅ | Shell로 동영상 길이 반환 |

### tpl_Media
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `GetVideoDuration` | ❌ 제외 | 하드코딩 경로, 특정 파일 전용 |
| `GetVideoLength` | ✅ | tpl_ExtApp과 동일 함수, 하나만 이식 |

### tpl_Procedure
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `BtCreateColorProcedure` | ❌ 제외 | GetTwbRange 시트 종속 |
| `CheckProc` | ❌ 제외 | ThisWorkbook.VBProject 의존, Trust Center 설정 필요 |
| `AddProcedure` | ❌ 제외 | ThisWorkbook.VBProject 의존 |
| `DelProcedure` | ❌ 제외 | ThisWorkbook.VBProject 의존 |
| `ReplaceModuleLine` | ❌ 제외 | ThisWorkbook.VBProject 의존 |
| `ReplaceAllLinesInModule` | ❌ 제외 | ThisWorkbook.VBProject 의존 |
| `ReplaceTextInModule` | ❌ 제외 | ThisWorkbook.VBProject 의존 |

### tpl_ReplaceText
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `ReplaceText` | ❌ 제외 | GetTwbRange 시트 종속 |
| `ConvertToExcelSerialDate` | ✅ | 날짜 → 엑셀 시리얼 변환 |
| `ExtractValues` | ✅ | 정규식으로 값 추출 |

### tpl_Tools
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `SetHotKeys` | ❌ 제외 | 특정 매크로명 하드코딩 |
| `BackGroundColor` | ❌ 제외 | GetTwbRange 종속 |
| `FontColor` | ❌ 제외 | GetTwbRange 종속 |
| `FindAndModifyShapes` | ❌ 제외 | 특정 텍스트 하드코딩 |
| `ChangeSizeForSpecificTextShapes` | ❌ 제외 | 특정 텍스트 하드코딩 |
| `CheckSelectionType` | ✅ | 선택 개체 타입 확인 |
| `ResetDB` | ❌ 제외 | 특정 DB 변수 종속 |
| `ResetAutoNumber` | ❌ 제외 | DAO, 특정 테이블 하드코딩 |
| `ChangeAllShapesFont` | ❌ 제외 | 폰트명 하드코딩 |
| `SetAllUserFormFontsToPretendard` | ❌ 제외 | 폰트명 하드코딩, VBProject 의존 |
| `WaitMs` | ✅ | ms 단위 대기 |
| `SyncCodeNamesToSheetNames` | ❌ 제외 | VBProject/VBIDE 의존, Trust Center 설정 필요 |
| `CleanCodeName` | ❌ 제외 | SyncCodeNamesToSheetNames 전용 내부 함수, 함께 제외 |

### tpl_Validation
| 함수 | 이식 여부 | 비고 |
|---|---|---|
| `EvaluateFormula` | ✅ | 수식 문자열 평가 |
| `SetIfValTrue` | ✅ | 유효성 통과 시 값 설정 |
